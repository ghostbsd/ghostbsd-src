# $FreeBSD$

BINDIR=/libexec/rc/bin

SUBDIR= checkpath
SUBDIR+= einfo
SUBDIR+= fstabinfo
SUBDIR+= get_options
SUBDIR+= is_newer_than
SUBDIR+= is_older_than
SUBDIR+= mountinfo
SUBDIR+= on_ac_power
SUBDIR+= rc-depend
SUBDIR+= service_starting
SUBDIR+= shell_var
SUBDIR_PARALLEL=

.include <bsd.subdir.mk>

